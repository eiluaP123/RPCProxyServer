#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <iostream>
#include <map>

using std::string;

void add_page(string url, string data, size_t len_data, size_t cache_size);

string search_page(string url);
